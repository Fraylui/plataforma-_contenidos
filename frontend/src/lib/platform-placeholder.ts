/**
 * Placeholder de identidad mientras no existe el módulo Identity/Config
 * (CONTEXTO.md sección 14). Cuando exista `platformSettings` en el backend,
 * este archivo se elimina y el layout/metadata lo consumen desde ahí.
 */
export const platformPlaceholder = {
  name: "Plataforma de Contenidos",
  description:
    "Sistema de gestión y distribución de contenidos digitales.",
} as const;
