package pe.plataformacontenidos.directory;

/** Tipo estructural del negocio (CONTEXTO.md sección 6) — eje de filtrado propio del Directorio, distinto de la categoría editorial (sección 4). */
public enum BusinessType {
    RESTAURANT,
    HOTEL,
    SERVICE,
    SHOP,
    OTHER
}
