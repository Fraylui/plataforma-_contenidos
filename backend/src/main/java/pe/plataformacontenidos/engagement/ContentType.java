package pe.plataformacontenidos.engagement;

/** Tipos de contenido publicable (CONTEXTO.md sección 3) que pueden recibir "me gusta" — ver ContentLike. */
public enum ContentType {
    ARTICLE,
    PLACE,
    EVENT,
    GALLERY,
    BUSINESS
}
