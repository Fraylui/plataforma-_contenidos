package pe.plataformacontenidos.engagement;

/** Los 6 tipos de contenido publicable (CONTEXTO.md sección 3) que pueden recibir "me gusta" — ver ContentLike. */
public enum ContentType {
    ARTICLE,
    PLACE,
    EVENT,
    GALLERY,
    REVIEW,
    BUSINESS
}
